new AccessCondition<{{InstitutionStore}}, {{WorkflowStore}}, {{OrganisationStore}}, {{RoleStore}}, {{ParameterStore}}>(){{#if Precondition}}
                  .Allow((i, w, o, r, a) => { {{{ Precondition }}} }){{/if}}{{#each Postconditions}}
                  .Action({{#if Condition}}(i, w, o, r, a) => { {{{ Condition }}} }, {{/if}}(i, w, o, r, a) => { {{{ Action }}}; } ){{/each}}